# Text-to-video

Toy-first local families for `text-to-video`.

## Families

- `zeroscope_t2v` -> `build_zeroscope_t2v_text_to_video(...)`
- `modelscope_t2v` -> `build_modelscope_t2v_text_to_video(...)`
- `lavie_t2v` -> `build_lavie_t2v_text_to_video(...)`
- `motion_prior_t2v` -> `build_motion_prior_t2v_text_to_video(...)`
- `cascade_t2v` -> `build_cascade_t2v_text_to_video(...)`
- `control_t2v` -> `build_control_t2v_text_to_video(...)`
- `transformer_t2v` -> `build_transformer_t2v_text_to_video(...)`
- `diffusion_t2v` -> `build_diffusion_t2v_text_to_video(...)`
- `prompt_t2v` -> `build_prompt_t2v_text_to_video(...)`
- `mamba_t2v` -> `build_mamba_t2v_text_to_video(...)`
