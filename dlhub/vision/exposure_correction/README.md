# Exposure correction

Compact-first local families for `exposure correction`.

## Families

- `gamma_exposure` -> `build_gamma_exposure_exposure_corrector(...)`
- `tone_exposure` -> `build_tone_exposure_exposure_corrector(...)`
- `histogram_exposure` -> `build_histogram_exposure_exposure_corrector(...)`
- `curve_exposure` -> `build_curve_exposure_exposure_corrector(...)`
- `contrast_exposure` -> `build_contrast_exposure_exposure_corrector(...)`
- `transformer_exposure` -> `build_transformer_exposure_exposure_corrector(...)`
- `prompt_exposure` -> `build_prompt_exposure_exposure_corrector(...)`
- `dual_exposure` -> `build_dual_exposure_exposure_corrector(...)`
- `retinex_exposure` -> `build_retinex_exposure_exposure_corrector(...)`
- `mamba_exposure` -> `build_mamba_exposure_exposure_corrector(...)`
