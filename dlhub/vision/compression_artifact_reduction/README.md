# Compression artifact reduction

Compact-first local families for `compression artifact reduction`.

## Families

- `jpeg_car` -> `build_jpeg_car_artifact_reducer(...)`
- `dct_car` -> `build_dct_car_artifact_reducer(...)`
- `residual_car` -> `build_residual_car_artifact_reducer(...)`
- `dual_car` -> `build_dual_car_artifact_reducer(...)`
- `frequency_car` -> `build_frequency_car_artifact_reducer(...)`
- `transformer_car` -> `build_transformer_car_artifact_reducer(...)`
- `diffusion_car` -> `build_diffusion_car_artifact_reducer(...)`
- `prompt_car` -> `build_prompt_car_artifact_reducer(...)`
- `mamba_car` -> `build_mamba_car_artifact_reducer(...)`
- `iterative_car` -> `build_iterative_car_artifact_reducer(...)`

