# Sign Digit Classification

Compact-first local families for `sign digit classification`.

## Families

- `cnn_sign_digit` -> `build_cnn_sign_digit_sign_digit_classifier(...)`
- `region_sign_digit` -> `build_region_sign_digit_sign_digit_classifier(...)`
- `attention_sign_digit` -> `build_attention_sign_digit_sign_digit_classifier(...)`
- `skeleton_sign_digit` -> `build_skeleton_sign_digit_sign_digit_classifier(...)`
- `transformer_sign_digit` -> `build_transformer_sign_digit_sign_digit_classifier(...)`
- `prompt_sign_digit` -> `build_prompt_sign_digit_sign_digit_classifier(...)`
- `contrastive_sign_digit` -> `build_contrastive_sign_digit_sign_digit_classifier(...)`
- `graph_sign_digit` -> `build_graph_sign_digit_sign_digit_classifier(...)`
- `efficient_sign_digit` -> `build_efficient_sign_digit_sign_digit_classifier(...)`
- `mamba_sign_digit` -> `build_mamba_sign_digit_sign_digit_classifier(...)`
