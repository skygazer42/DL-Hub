
    # Video frame interpolation

    Compact-first local families for `video frame interpolation`.

    ## Families

    - `sepconv_interp` -> `build_sepconv_interp_interpolator(...)`
- `super_slomo` -> `build_super_slomo_interpolator(...)`
- `dain_baseline` -> `build_dain_baseline_interpolator(...)`
- `rife_baseline` -> `build_rife_baseline_interpolator(...)`
- `flavr_baseline` -> `build_flavr_baseline_interpolator(...)`
- `vfi_former` -> `build_vfi_former_interpolator(...)`
- `amt_interp` -> `build_amt_interp_interpolator(...)`
- `ifrnet_baseline` -> `build_ifrnet_baseline_interpolator(...)`
- `ema_vfi` -> `build_ema_vfi_interpolator(...)`
- `mamba_vfi` -> `build_mamba_vfi_interpolator(...)`
