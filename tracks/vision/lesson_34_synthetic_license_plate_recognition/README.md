# Lesson 34: Synthetic License Plate Recognition

This lesson frames license plate recognition as a fixed-length sequence prediction problem. Each
synthetic plate is rendered from deterministic character templates, and a compact CNN reads one
token per plate slot.

## What It Teaches

- synthetic OCR-style data generation without external assets
- sequence prediction from pooled visual features
- exact-match sequence accuracy for fixed-length recognition tasks
