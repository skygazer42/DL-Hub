# Tracks（课程轨道）

这个目录下是统一风格的课程内容：每个轨道（track）是一条学习路径，每节课（lesson）都有一致的结构与运行方式。

推荐从 `tracks/foundations/` 与 `tracks/vision/` 开始；如果你对图或文本更感兴趣，也可以从 `tracks/gnn/` / `tracks/nlp/` / `tracks/llm/` 的 toy lesson 直接上手（无需下载数据）。
