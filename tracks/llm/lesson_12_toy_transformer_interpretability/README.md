# Lesson 12: Toy Transformer Interpretability

Tiny decoder-only LM lesson focused on two interpretability tools:

- Attention-map inspection from a single causal self-attention block.
- Token-saliency inspection from gradient norms over input embeddings.

The dataset is synthetic and lightweight so training and inspection run quickly on CPU.
