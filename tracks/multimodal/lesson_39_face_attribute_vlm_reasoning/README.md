# Lesson 39: Face Attribute VLM Reasoning

This lesson builds a compact multimodal reasoner that combines a synthetic face crop and a short
attribute query, then predicts whether the queried face attribute is present.

## What It Teaches

- synthetic face generation with deterministic identity-linked attributes
- compact vision-language fusion for query-conditioned attribute reasoning
- lightweight binary training loops with metrics logging and checkpointing
