# 安全政策

## 支持范围

DL-Hub 目前没有多版本安全维护承诺。安全报告会以最新 `main` 为主要核对目标；如果问题只出现在
旧提交或本地分支，请尽量说明它能否在最新 `main` 复现。项目不承诺固定的确认或修复时限，
维护者会根据影响、可复现性和可用维护时间协调处理。

## 私下报告漏洞

不要用公开 Issue、Discussion 或 Pull Request 披露尚未修复的漏洞或利用细节。

1. 打开仓库的 **Security → Advisories** 页面。如果页面显示 **Report a vulnerability**，
   请优先使用该表单；这个入口只有在仓库启用了 GitHub 私密漏洞报告时才会出现。
2. 如果没有该入口，请发送邮件到仓库已经公开列出的维护者地址
   `207829897@qq.com`，主题以 `[DL-Hub security]` 开头。
3. 在维护者确认可以公开之前，请继续使用同一私密渠道沟通。

报告尽量包含：

- 受影响的提交、版本、文件或入口；
- 安全影响和攻击前提；
- 已脱敏的最小复现步骤或概念验证；
- 操作系统、Python/PyTorch 版本和安装方式；
- 已知缓解方式或修复建议（可选）。

不需要提供姓名、电话或其他额外个人信息。请删除访问令牌、Cookie、私有仓库地址、用户名、
主目录路径和个人数据；不要发送私有数据集、专有模型权重或真实用户样本。优先使用 synthetic
输入、最小文本片段和文件哈希。若凭据已经暴露，应先撤销或轮换，再报告问题。

## Checkpoint 与 pickle 风险

PyTorch checkpoint 可能包含 pickle 数据，恶意文件可能在反序列化时执行任意代码。
`dlhub.checkpoint.load_checkpoint()` 默认使用受限的 `weights_only=True` 加载，并且安全加载失败时
不会自动降级。`allow_unsafe_legacy=True` 只用于来源完全可信的旧 checkpoint；它会启用
unrestricted pickle，不能用于下载文件、第三方附件或来源不明的权重。

发现可疑 checkpoint 时，不要加载它，也不要把二进制文件附到公开 Issue。先报告来源、哈希、
触发路径和最小观察结果，等待维护者确认后再决定传输方式。

## 哪些问题属于安全报告

包括但不限于：

- 非预期代码执行、危险反序列化或命令注入；
- 路径穿越、越界文件写入或不安全的产物覆盖；
- 凭据、私有数据或其他敏感信息泄露；
- GitHub Actions、打包或依赖链中可被实际利用的供应链问题；
- 能绕过既有安全边界的输入或配置。

以下内容通常应使用普通 Issue 模板：

- Lesson 功能建议、训练报错或性能问题；
- 指标差异、论文机制讨论或 Model Zoo 保真度分类争议；
- 对超大输入进行预期中的高资源训练；
- 仅表明依赖版本过旧、但没有说明本仓库中可达影响的报告。

如果不确定问题是否具有安全影响，请先使用私密渠道。

## 协调与安全研究边界

维护者可能要求更小的复现、影响说明或补丁建议，并会通过原私密渠道协调修复与披露。
本项目不预先承诺漏洞奖励、CVE 或发布日期。测试时请避免访问第三方账户、破坏数据、持续影响
服务可用性、开展社会工程或保留无关数据。
